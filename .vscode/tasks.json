// use cmake list,algorithm use it
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "g++",
            "type": "shell",
            "command": "sh ${workspaceFolder}/build.sh",
            "group": "build",
        }
    ]
}
//use vscode default task, basic file use it
/**
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "createBuild",
            "type": "shell",
            "command": "mkdir -p ${workspaceFolder}/build/",
        },
        {
            "label": "g++",
            "type": "shell",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/build/main"
            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "dependsOn": [
                "createBuild"
            ]
        }
    ]
}
**/