include_directories(${PROJECT_SOURCE_DIR}/include ${PROJECT_SOURCE_DIR}/src/algorithm/tree)
aux_source_directory(algorithm SRC_LIST)
aux_source_directory(algorithm/tree SRC_TREE)
add_executable(main ${SRC_LIST} ${SRC_TREE})
set(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/build)
